apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-mesh-microfrontend.fullname" . }}
  labels:
    {{- include "platform-mesh-microfrontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "platform-mesh-microfrontend.selectorLabels" . | nindent 4 }}
