apiVersion: v1
kind: Service
metadata:
  name: {{ include "openkcm-ui.fullname" . }}
  labels:
    {{- include "openkcm-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "openkcm-ui.selectorLabels" . | nindent 4 }}
